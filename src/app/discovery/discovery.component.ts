import { Component } from '@angular/core';

// NOTE: Do NOT label this as "AI" anywhere in the UI — call it "Discover Movies"
// Several group members are AI-averse. The feature should feel curated, not algorithmic.
@Component({
  selector: 'app-discovery',
  templateUrl: './discovery.component.html',
  styleUrls: ['./discovery.component.scss'],
})
export class DiscoveryComponent {}
